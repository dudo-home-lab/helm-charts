apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudflare-tunnel-remote.fullname" . }}-test-connection"
  labels:
    {{- include "cloudflare-tunnel-remote.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudflare-tunnel-remote.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
